## Notes

- #sujet/themea: une réunion doit être organisée par l'équipe projet rapidement
- #sujet/themeb Flux cible: un référent métier a validé oralement la version rapide que l'équipe applicative peut fournir, il doit vérifier un point (cf email). 
	- Priorités pour la version complète (début 2027):
	- Les **4 Events** avec des durées pouvant atteindre **150 jours** doivent être prioritaires.
	- Il faut **conserver les valeurs du compteur en cas de données manquantes**.
	- Une réunion est nécessaire pour valider cette solution **avant fin 2027**.
-  #sujet/export propose un script à part pour regrouper les fichiers 

---
## Actions

- [ ] #sujet/themeb Demander des nouvelles au référent à la prochaine réunion concernant le point manquant (cf email) 📅 2026-07-16 
- [ ] #sujet/export Organiser un point avec l'équipe applicative et les chefs de projet au sujet de la solution d'export pour planning 📅 2026-07-15 
- [ ] #sujet/themea **Finaliser et valider le modèle de données** après consultation avec l'équipe pour alignement et cohérence 📅  2026-07-15
---
## Tâches des 7 prochains jours

```dataviewjs
const currentFile = dv.current().file.name;

// Adapté à des notes nommées YYYY-MM-DD
const start = dv.date(currentFile);
const end = start.plus({ days: 7 });

const tasks = dv.pages()
    .file.tasks
    .where(t =>
        !t.completed &&
        t.due &&
        t.due >= start &&
        t.due <= end
    );

if (tasks.length === 0) {
    dv.paragraph("✅ Aucune tâche prévue sur les 7 prochains jours.");
} else {
    dv.taskList(tasks);
}
```
